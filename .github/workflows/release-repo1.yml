on:
  workflow_call:
    inputs:
      tag:
        description: 'Tag to release'
        required: true
        type: string

name: release-repo1
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: test-repo1
        run: |
          echo "Test Repo 1"